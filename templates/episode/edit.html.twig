{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <h1 class="title">Edit Episode</h1>
    <div class="btn-link-wrapper">
        {{ include('episode/_delete_form.html.twig') }}
    </div>
    
    {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('episode_index') }}"><button type="button" class="btn btn-warning">Back to list</button></a>
{% endblock %}
